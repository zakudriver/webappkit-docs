# 快速开始

```bash
git clone https://git.querycap.com/fe/webappkit.git

cd webappkit && pnpm i

# 编译 monobundle 打包脚本
make bootstrap.monobundle

# 编译 babel 插件
make build.babel-plugins

# 编译 构建工具
make bootstrap

# 启动 sg 项目
npx devkit dev sg
```

